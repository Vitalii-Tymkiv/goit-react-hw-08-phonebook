import { RegisterForm } from 'components/RegisterForm/RegisterForm';

const Register = () => {
  return (
    <main>
      <title>Sign up</title>
      <RegisterForm />
    </main>
  );
};

export default Register;
